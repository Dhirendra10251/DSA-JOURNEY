class Solution {
public:
    vector<int> replaceElements(vector<int>& nums) {
        int maxi = -1;
        for (int i = nums.size()-1; i >= 0; i--) {
            int newMaxi = max(maxi, nums[i]);
            nums[i] = maxi;
            maxi = newMaxi;
        }
        return nums;
    }
};
